$kind: http-request
name: Create User
url: "{{baseUrl}}/api/users"
method: POST
headers:
  Accept: application/json
  Content-Type: application/json
  x-api-key: "{{apiKey}}"
body:
  type: json
  content: |-
    {
      "name": "Trang Nguyen",
      "job": "QA"
    }
order: 3719590225033836
